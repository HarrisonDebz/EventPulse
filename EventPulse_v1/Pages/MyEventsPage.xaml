<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="EventPulse_v1.Pages.MyEventsPage"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:vm="clr-namespace:EventPulse_v1.ViewModels"
    BackgroundColor="{StaticResource DarkBackground}">

    <ContentPage.BindingContext>
        <vm:MyResponsesViewModel />
    </ContentPage.BindingContext>

    <CollectionView ItemsSource="{Binding MyEvents}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <Border Style="{StaticResource Card}">
                    <Grid ColumnDefinitions="*,Auto">
                        <StackLayout>
                            <Label Text="{Binding Title}" TextColor="{StaticResource TextPrimary}" />
                            <Label Style="{StaticResource SmallSecondary}" Text="{Binding WhenSummary}" />
                        </StackLayout>
                        <Button
                            Command="{Binding Source={RelativeSource AncestorType={x:Type vm:MyRsvpsViewModel}}, Path=UnrsvpCommand}"
                            CommandParameter="{Binding .}"
                            Text="Remove" />
                    </Grid>
                </Border>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
